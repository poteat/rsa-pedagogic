/**
 * Maximum number of attempts to generate a given random sample according to
 * prespecified conditions. Put in place to enable proof that the algorithm
 * eventually halts.
 */
export const maxRandomSamplings = 1000;
